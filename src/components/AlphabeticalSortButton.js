import React from 'react';

const AlphabeticalSortButton = (props) => {
    return <button onClick={props.onClick} className="btn btn-info">Alfabetisk ordning</button>
}

export default AlphabeticalSortButton;